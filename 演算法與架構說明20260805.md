# 演算法與架構說明（2026-08-05）

本系統結合左側交易（抄底）、右側交易（順勢突破）以及總體經濟位階判定，提供具機構級防阻擋機制與高速運算能力的量化分析平台。系統重點在於過濾市場雜訊，將主觀圖表轉換為客觀的量化評分。

## 壹、核心演算法邏輯解析（獨立說明）

系統內建四大獨立演算法模組：

### 演算法一：BottomReversalStrategy（左側低檔強力翻轉模組）

目的：在股價下跌趨勢末端，精準捕捉主力進場的「見底特徵」。

運算邏輯與條件：

- 位階絕對隔離（趨勢濾網）
  - 系統計算 20 日乖離率（`BIAS20`）。只有在 `BIAS20 <= 0`（收盤價在月線之下或貼齊月線）時才啟動給分機制；若在高檔出現相同型態，系統會忽略。

- 量能放大係數（Volume Multiplier）
  - 當日成交量 / 20 日均量，並將該乘數限制在 `0.5` 到 `3.0` 之間，以避免無量反彈或極端爆量造成過度評分。

- 型態判定與計分：
  - 型態一：低檔長下影線（釘子線）
    - 條件：下影線長度 > 實體的 1.5 倍，且下影線佔整根 K 線總震幅的 40% 以上。
    - 計分公式：`7 × (下影線長度 ÷ 總震幅) × 量能放大係數`（理論極限約 21 分）。

  - 型態二：低檔實體長紅 K（破底翻）
    - 條件：收盤 > 開盤（實體收紅），且單日漲幅 ≥ 2.5%。
    - 計分公式：`5 × 量能放大係數`（固定基礎分 5 分，乘以量能放大係數）。

### 演算法二：VCPStrategy（右側波動收斂型態模組）

目的：在多頭趨勢中捕捉籌碼沉澱、洗盤結束、即將進入主升段突破的壓縮末端。

運算邏輯與條件：系統必須同時滿足三大嚴格條件，才會給予 VCP 分數：

- 條件 A（多頭排列）：收盤價 > 月線（`MA20`），且月線 > 季線（`MA60`）。
- 條件 B（量能萎縮）：當日成交量 < 20 日均量。
- 條件 C（價格壓縮）：布林通道帶寬 = `(上軌 - 下軌) ÷ 月線 × 100`，需壓縮至 `10.0%` 以下。

計分系統（滿分 20 分）：

- 量縮得分（佔 10 分）：`10 × (1 - 當日量 / 月均量)`。
- 價格緊密得分（佔 10 分）：`10 × (10 - 帶寬) / 10`。

### 演算法三��DivergenceStrategy（多級別底背離判定模組）

目的：確認空方殺傷力衰竭，避免在下跌中繼時接到「半山腰的刀子」。

運算邏輯與條件：

- 雙重低點確立：系統將歷史資料分為「近波（`recent_w`）」與「前波（`older_w`）」。
- 嚴格轉折判定（Pivot Point）：若使用者開啟嚴格模式，系統會檢查每一個低點是否滿足「左 X 根、右 Y 根皆不破底」以確認為實質轉折點。

背離三大鐵律：

1. 價格破底：近波低點價格 < 前波低點價格（p2 > p1）。
2. 指標不破底：近波指標值（如 KD 的 K，或 MACD 的快線） > 前波指標值（i2 < i1）。
3. 死叉驗證（Dead Cross）：兩波低點之間，指標需曾出現「快線向下交叉慢線」的死叉，確認這兩個低點屬於兩個獨立下跌波段。

### 演算法四：MarketRegimeFilter（大盤位階與動態濾網）

目的：辨識總體市場環境，決定系統當下應採取的操作策略（積極作多或保守觀望）。

運算邏輯：

- 抓取 `^TWII`（加權指數）歷史資料，將指數收盤價作為系統評估風險的定錨點。

環境判定：

- 多頭排列：基礎指數 > `MA20`，且 `MA20` > `MA60`。
- 空頭弱勢：基礎指數 < `MA20`，且 `MA20` < `MA60`。
- 震盪整理：均線糾結，未達上述兩種極端狀態。

## 貳、系統工程架構與執行流程

除了核心演算法，系統在資料流與執行效能上進行企業級優化：

### 1. 防阻擋與高併發連線機制（Requests Session & Retry）

- 由於 Yahoo Finance API 對短時間高頻請求有嚴格 Rate Limit，系統放棄單純的 `yf.download` 多執行緒方式，改為建立自訂的 `requests.Session()`。
- 並加入 `urllib3.util.retry.Retry`：遇到 HTTP 429（Too Many Requests）或伺服器錯誤時，自動採用指數退避（Exponential Backoff）重試 3 次（延遲 1.5s、3s、4.5s）。
- 偽裝 `User-Agent` 模擬真實瀏覽器以降低被封鎖機率。

### 2. 記憶體級快取架構（@st.cache_data）

- 台股清單快取（`get_all_tw_stocks`）：每日只爬取一次證交所與櫃買中心網頁，並以 TTL=86400 秒快取結果。
- 股票名稱快取（`get_tw_stock_name`）：避免重複發送網頁解析請求，TTL=3600 秒。
- 單檔歷史資料快取（`get_stock_data`）：在 Tab 1 查詢時，一小時內重複查詢將直接從記憶體快取讀取以達到低延遲。

### 3. 全市場掃描雙軌制（Tab 2 執行流程）

使用者點擊掃描後，系統運作流程如下：

- 階段一（區間粗篩）
  - 依使用者選擇的模式（全部 / 底部翻轉 / VCP），將全市場約 1700 檔股票切分為每 100 檔一組（chunk）進行歷史資料下載。
  - 使用 Pandas 向量化運算（vectorization）進行高速矩陣運算，計算每檔股票每日的 K 線分數與 VCP 分數。
  - 特徵擷取：在使用者指定的掃描區間內，找出「（反轉分數 + VCP 分數）最高」的該日作為觸發日；若該分數滿足設定門檻，則納入候選名單。

- 階段二（深度背離檢驗）
  - 對候選名單使用階段一暫存的日 K 線進行日級別背離比對，並下載 60 分鐘 K 線進行短線級別的背離比對，以提高檢驗精度。

- 動態結果合成
  - 依雙策略與雙級別背離的達成狀況，自動組裝「演算法建議結果」標籤，並依分數高低排序後渲染為 DataFrame，供使用者查閱與篩選。

（部分原始內容在來源檔中以省略號標示，若需完整展開說明或加入範例、公式推導與 pseudocode，我可以繼續補充。）
