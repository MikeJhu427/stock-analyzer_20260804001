​本系統結合了左側交易（抄底）、右側交易（順勢突破）以及總體經濟位階判定，是一套具備機構級防阻擋機制與高速運算能力的量化分析平台。以下將系統拆解為「核心演算法」與「系統工程架構」兩大部分進行深度說明。
​壹、核心演算法邏輯解析（獨立說明）
​系統的靈魂在於過濾市場雜訊，將主觀的圖表轉換為客觀的量化評分。以下為系統內建的四大獨立演算法模組：
​演算法一：BottomReversalStrategy (左側低檔強力翻轉模組)
​目的： 在股價下跌趨勢的末端，精準捕捉主力真金白銀進場點火的「見底特徵」。
運算邏輯與條件：
​位階絕對隔離 (趨勢濾網)：
系統強制計算 20 日乖離率（BIAS20）。只有在 BIAS20 <= 0（即收盤價在月線之下或貼齊月線）時，才會啟動給分機制。若在高檔出現相同型態，系統一律判定為 0 分，避免追高。
​量能放大係數 (Volume Multiplier)：
將當日成交量除以 20 日均量，並將該乘數限制在 0.5 到 3.0 倍之間。這能確保無量反彈會被扣分，而極端異常的爆量（如 10 倍天量）最高也只會將分數放大 3 倍，防止因單一極端值導致分數失真。
​型態一：低檔長下影線 (釘子線)
​條件： 下影線長度大於實體的 1.5 倍，且下影線佔整根 K 線總震幅的 40% 以上。
​計分公式： 7 × (下影線長度 ÷ 總震幅) × 量能放大係數。下影線越長、佔比越極端，分數越高（理論極限約 21 分）。
​型態二：低檔實體長紅K (破底翻)
​條件： 實體收紅（收盤 > 開盤），且單日漲幅大於或等於 2.5%。
​計分公式： 5 × 量能放大係數。給予固定基礎分 5 分，直接與爆量程度相乘。
​演算法二：VCPStrategy (右側波動收斂型態模組)
​目的： 捕捉多頭趨勢中，籌碼經過沉澱、洗盤，即將迎來主升段突破的壓縮末端。
運算邏輯與條件：
系統必須同時滿足三大嚴格條件，才會給予 VCP 分數：
​條件 A (多頭排列)： 收盤價大於月線 (MA20)，且月線大於季線 (MA60)。確保大趨勢向上。
​條件 B (量能萎縮)： 當日成交量必須「小於」20 日均量，確認洗盤末端賣壓已竭盡。
​條件 C (價格壓縮)： 布林通道帶寬（(上軌 - 下軌) ÷ 月線 × 100）必須壓縮至 10.0% 以下。
​計分系統 (滿分 20 分)：
​量縮得分 (佔 10 分)： 成交量相較於月均量越小，得分越高。公式：10 × (1 - 當日量/月均量)。
​價格緊密得分 (佔 10 分)： 布林帶寬越窄，得分越高。公式：10 × (10 - 帶寬) / 10。
​演算法三：DivergenceStrategy (多級別底背離判定模組)
​目的： 確認空方殺傷力實質衰竭，防止在下跌中繼站接到「半山腰的刀子」。
運算邏輯與條件：
​雙重低點確立： 系統將歷史資料分為「近波 (recent_w)」與「前波 (older_w)」。
​嚴格轉折判定 (Pivot Point)： 若使用者開啟嚴格模式，系統會檢查每一個低點是否滿足「左 X 根、右 Y 根」皆不破底的條件，確認其為實質轉折點。
​背離三大鐵律：
​價格破底： 近波低點價格 < 前波低點價格 (p2 > p1)。
​指標不破底： 近波指標值 (KD的K，或MACD的快線) > 前波指標值 (i2 < i1)。
​死叉驗證 (Dead Cross)： 兩波低點之間，指標必須曾經發生過「快線向下交叉慢線」的死叉訊號。這是為了確認這兩個低點屬於「兩個獨立的下跌波」，而非同一個波段的鈍化延伸。
​演算法四：MarketRegimeFilter (大盤位階與動態濾網)
​目的： 辨識總體市場環境，決定當下是該積極作多還是保守觀望。
運算邏輯：
​抓取 ^TWII (加權指數) 歷史資料。
​將指數收盤價設定為核心的自動運算基準價值 (約當大台基礎)，以此作為系統評價系統風險的定錨點。
​環境判定：
​多頭排列：約當大台基礎 > MA20，且 MA20 > MA60。
​空頭弱勢：約當大台基礎 < MA20，且 MA20 < MA60。
​震盪整理：均線糾結，未達上述兩種極端狀態。
​貳、系統工程架構與執行流程
​除了核心演算法，本程式在資料流與執行效能上進行了企業級的優化：
​1. 防阻擋與高併發連線機制 (Requests Session & Retry)
​Yahoo Finance API 對短時間內的高頻請求有嚴格的 Rate Limit。
​系統捨棄了單純的 yf.download 多執行緒，改為建立自訂的 requests.Session()。
​加入 urllib3.util.retry.Retry：遇到 HTTP 429 (Too Many Requests) 或伺服器錯誤時，自動套用指數退避 (Exponential Backoff) 重試 3 次 (延遲 1.5s, 3s, 4.5s)。
​偽裝 User-Agent，模擬真實瀏覽器行為，大幅降低被封鎖的機率。
​2. 記憶體級快取架構 (@st.cache_data)
​台股清單快取 (get_all_tw_stocks)： 每日只爬取一次證交所與櫃買中心網頁，存入快取 (TTL=86400秒)。
​股票名稱快取 (get_tw_stock_name)： 避免同一個代號重複發送網頁解析請求，TTL=3600秒。
​單檔歷史資料快取 (get_stock_data)： 於 Tab 1 查詢時，一小時內重複查詢將瞬間從記憶體讀取，達到零延遲。
​3. 全市場掃描雙軌制 (Tab 2 執行流程)
​使用者點擊掃描後，系統運作流程如下：
​階段一 (區間粗篩)： 依照使用者選擇的模式（全部 / 底部翻轉 / VCP），將全市場 1700 多檔股票切分為每 100 檔一組 (Chunk) 進行歷史資料下載。
​高速矩陣運算： 利用 Pandas 的向量化運算 (Vectorization)，瞬間計算每一檔股票每一天的 K 線分數與 VCP 分數。
​特徵擷取： 在使用者指定的「掃描區間」內，找出「(反轉分數 + VCP分數) 最高」的那一天作為觸發日。若最高分滿足設定門檻，則將該股票納入候選名單，並將日 K 線資料暫存 (_Daily_DF)。
​階段二 (深度背離檢驗)： 針對入選的候選名單，直接調用階段一暫存的日 K 線進行日級別背離比對；同時下載 60 分鐘 K 線，進行短線級別的背離比對。
​動態結果合成： 依照雙策略與雙級別背離的達成狀況，自動組裝「演算法建議結果」標籤，最後依照分數高低排序並渲染為 DataFrame，供使用者查閱與匯出 CSV。
