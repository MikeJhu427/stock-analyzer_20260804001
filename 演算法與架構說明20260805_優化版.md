# 📈 台股量化與基本面雙軌分析平台 - 演算法與架構說明 v5.0

## 📋 系統總覽

本系統結合了**左側交易（抄底）**、**右側交易（順勢突破）**、**多級別指標共振**以及**機構級財報三表交叉勾稽**，是一套具備防阻擋機制、高速運算能力與財務防雷引擎的全方位量化分析平台。

系統的核心在於**過濾市場雜訊與單一指標盲點**，將主觀的 K 線圖表與死板的財報數字，轉換為客觀的量化評分與立體的情境診斷。

---

## 壹、核心技術面演算法解析

### 🔴 演算法一：BottomReversalStrategy (左側低檔強力翻轉模組)

#### 目的
在股價下跌趨勢的末端，精準捕捉主力真金白銀進場點火的「見底特徵」。

#### 運算邏輯與條件
1. **位階絕對隔離**：`BIAS20 ≤ 0` (收盤價在月線之下)，確保只在低檔區域捕捉，高檔出現相同型態一律不給分。
2. **量能放大係數**：當日成交量 ÷ 20日均量 (限制在 0.5 ~ 3.0 倍之間)。無量反彈扣分，極端爆量最多加至 3 倍。
3. **型態一 (低檔釘子線)**：下影線 > 實體 1.5 倍且佔總震幅 40% 以上。公式：`7 × (下影線長度 ÷ 總震幅) × 量能放大係數`
4. **型態二 (低檔破底翻)**：單日實體收紅且漲幅 ≥ 2.5%。公式：`5 × 量能放大係數`

---

### 🟢 演算法二：VCPStrategy (右側波動收斂型態模組)

#### 目的
捕捉多頭趨勢中，籌碼經過沉澱、洗盤，即將迎來主升段突破的壓縮末端。

#### 運算邏輯與條件 (滿分 20 分)
必須同時滿足三大嚴格條件：
1. **多頭排列**：`收盤價 > MA20` 且 `MA20 > MA60`
2. **量縮得分 (10分)**：`10 × (1 - 當日量/月均量)`，確認洗盤末端賣壓竭盡。
3. **價格壓縮得分 (10分)**：`10 × (10 - 布林帶寬) / 10`，帶寬需 ≤ 10.0%。

---

### 🟡 演算法三：IndicatorResonanceStrategy (雙指標共振模組)

#### 目的
結合 KD 區間濾網與 MACD 零軸波段切割，確認底部動能共振，避免單一指標騙線。

#### 運算邏輯與條件
1. **動態波段切割**：自動以 MACD 柱狀圖的零軸作為分界，精確比對「近波」與「前波」的絕對低點。
2. **黃金交叉確認**：要求 KD 與 MACD 在指定的 N 天內雙雙發生黃金交叉。
3. **動能加分**：基礎共振達標給 10 分，額外根據 MACD 柱狀圖斜率 (Momentum) 進行動能加分 (最多加 5 分，滿分 15 分)。
4. **進場位置防護**：若觸發時 K 值過高，系統會發出「位置偏高」的警告，避免追高風險。

---

### 🔵 演算法四：DivergenceStrategy (多級別背離判定模組)

#### 目的
確認空方殺傷力實質衰竭，防止在下跌中繼站接到「半山腰的刀子」。

#### 運算邏輯與條件
1. **雙重低點比對**：比對近波 (recent_w) 與前波 (older_w) 的轉折低點。
2. **嚴格 Pivot 轉折**：支援「左 X 根、右 Y 根皆不破底」的嚴格 V 型轉折判定。
3. **背離三大鐵律**：
   - 價格破底 (p2 < p1 或依照設定容許不破底)
   - 指標不破底 (i2 > i1)
   - 死叉驗證 (確保為兩個獨立波段)
4. **多級別穿透**：同時支援 **日K級別** 與 **60分K級別** 雙重背離掃描。

---

## 貳、基本面引擎：財報三表交叉診斷 (FinancialAuditStrategy V5)

#### 目的
捨棄傳統「單一指標異常＝地雷」的過度簡化邏輯。建立「損益表、資產負債表、現金流量表」的立體關聯，精準辨識「真地雷」與「被誤判的營運擴張」。

#### 核心防誤判機制：合約負債 (Contract Liabilities)
- **智慧 Fallback 抓取**：系統內建強大 Alias Mapping，自動適應各公司財報不同命名 (如 Contract Liabilities, Deferred Revenue, 客戶預付款等)。
- **時間尺度檢驗**：不依賴單季波動，強制比對「最新、4季趨勢、8季歷史基準」，判斷是短期雜訊還是長期趨勢。
- **正向營運資金循環 (風險抵銷)**：
  - 傳統盲點：`存貨增加 = 地雷`
  - V5 引擎判定：若 `存貨增加` + `合約負債(客戶預付)增加` + `營收同步成長` + `CFO改善` → 系統會判定為 **「強烈需求帶動之健康備貨」**，並自動降低風險分數。

#### 雙軌評分系統 (0~100分)
| 軌道 | 滿分 | 評分重點 |
|------|------|---------|
| 🏆 **體質分數 (Quality)** | 100 | 高毛利、高營益率、獲利高含金量 (CFO>NI)、客戶預付款帶動營收。分數越高越具護城河。 |
| 🛡️ **風險分數 (Risk)** | 100 | 存貨周轉失控 (DIO > 150天)、應收帳款過長、黑字倒閉風險 (NI>0 但 CFO 連續為負)。分數越高地雷機率越大。 |

---

## 參、系統工程架構與執行流程

### 🔧 企業級優化設計

#### 1️⃣ 防阻擋與高併發連線機制 (Session & Exponential Backoff)
- **問題**：Yahoo Finance API 對高頻請求有 Rate Limit。
- **解法**：自訂 `requests.Session()` 加入 `urllib3.util.retry.Retry` (1.5s → 3s → 4.5s 退避)，並動態偽裝 User-Agent。

#### 2️⃣ 記憶體級快取架構 (@st.cache_data)
- `get_all_tw_stocks()`: 24 小時 TTL，每日只爬取一次證交所網頁。
- `get_stock_data()`: 1 小時 TTL，確保 Tab 1 的頻繁切換與查詢達到零延遲。

#### 3️⃣ 全市場智慧掃描管線 (Tab 2 工作流)
1. **分組批次 (Chunking)**：全市場 1700+ 檔標的分為每 40 檔一組進行異步下載。
2. **時光機回測**：支援「指定回測基準日」，自動截斷未來資料，驗證過去任意時間點的策略勝率。
3. **長天期運算**：入選標的自動觸發階段二下載，計算 120MA/240MA 空排與斜率、5/10/20/60MA 均線扣抵狀態。

---

## 肆、系統架構拓撲圖

```text
┌──────────────────────────────────────────────────────────┐
│              用戶交互層 (UI/Streamlit)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │   Tab 1     │  │   Tab 2     │  │   Tab 3     │      │
│  │  單檔技術   │  │  全市場掃描 │  │ 三表財報健檢│      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└──────────────────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────────────────┐
│            演算法層（五大核心引擎）                        │
│  ┌──────────────────────────────────────────────────┐   │
│  │  ① BottomReversalStrategy   (左側反轉捕捉)      │   │
│  │  ② VCPStrategy              (右側波動收斂)      │   │
│  │  ③ IndicatorResonanceStrategy (雙指標波段共振)  │   │
│  │  ④ DivergenceStrategy       (多級別背離判定)    │   │
│  │  ⑤ FinancialAuditStrategy V5(三表交叉與合約負債)│   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────────────────┐
│        優化層（工程加速機制）                             │
│  ┌────────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │  防阻擋機制     │  │  快取架構    │  │  向量計算   │  │
│  │ (Retry/Session)│  │(st.cache)    │  │(Pandas)    │  │
│  └────────────────┘  └──────────────┘  └────────────┘  │
└──────────────────────────────────────────────────────────┘
